function betterThanAverage(arr) {
    var sum = 0;
    for(var i=0; i < arr.length; i++){
        sum += arr[i]
    }
    return sum/arr.length;

}
var result = betterThanAverage([6, 8, 3, 10, -2, 5, 9]);
console.log(result);
//  // we expect back 4




function betterThanAverage(arr) {

    var count = 0
    for (var i = 0; i > sum/arr.length)
    return count;
}

